<%provide(:title, "Blog") %>
<div id="content">
<h1>Blog</h1>
<div id="post">
<% @articles.each do |article| %>
    <h2>
    <%= link_to article.title, article %>
    </h2>
    <p>
    <%= time_tag article.created_at, format: ("%B %d, %Y") %>
    </p>
<% end %>
</div>
</div>